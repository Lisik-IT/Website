<ul>
  <% routes.forEach((route) => { %>
  <li>
    <a href="<%- `${prefix}${route.url}` %>"><%- route.name %></a>
    <% if ( route.children) { %>
    <%- partial('menu', { routes: route.children, prefix }) %>
    <% } %>
  </li>
  <% }) %>
</ul>
